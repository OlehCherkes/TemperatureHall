# TempHall
Библиотека для обработки внтутренего датчика температуры и датчика Холла

## Использование
getTemperature();   // функция получения температуры °C
printTemperature(); // функция вывода значения температуры °C в терминал

getHall();                  // функция получения значений датчика Холла
printHall();                // функция вывода значений датчика Холла в терминал
ledSensor(uint16_t ledPin); // функция изминения яркости светодиода от значений датчика Холла

<a id="example"></a>
## Пример
#include <Arduino.h>
#include "TemperatureHall"

Temperature test1; //обьект класса датчика температуры
Hall test2;        // обьект класса датчика Холла

void setup()
{
  Serial.begin(9600);
}

void loop()
{ 
 test1.getTemperature();   // функция получения температуры °C
 test1.printTemperature(); // функция вывода значения температуры °C в терминал
 
 test2.getHall();     // функция получения значений датчика Холла
 test2.ledSensor(18); // функция изминения яркости светодиода от значений датчика Холла (в скобках указываем номер пина)
}
